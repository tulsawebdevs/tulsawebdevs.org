============
Installation
============

At the command line::

    $ easy_install tulsawebdevs.org

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv tulsawebdevs.org
    $ pip install tulsawebdevs.org
